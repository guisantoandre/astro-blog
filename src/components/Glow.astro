---
interface Props {
   color?: string;
   opacity?: number;
   class?: string;
}

const {
   color = "#575E6E",
   opacity = "0.15",
   class: className = "",
} = Astro.props;
---

<div
   class={`absolute -z-10 pointer-events-none blur-2xl ${className}`}
   style={`background: radial-gradient(ellipse at center, ${color} 15%, transparent 65%);
          opacity: ${opacity};`}
>
</div>